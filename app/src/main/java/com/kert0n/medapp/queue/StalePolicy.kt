package com.kert0n.medapp.queue

/**
 * Что делает команда, когда версия предусловия устарела (PLAN E3). Различие — в смысле
 * намерения: дельту расхода и абсолютную бронь чужое изменение не отменяет, и они готовятся
 * заново по свежему состоянию; описание, пересчёт, перенос и удаление чужая правка перекрывает,
 * и человек решает заново, глядя на новое состояние.
 */
enum class StalePolicy { REPREPARE, REFUSE }
